@AGENTS.md

# Workflow rules

- **Never merge a pull request without David's explicit approval.** Open PRs,
  let CI run, and report what's ready to merge (noting stacked-PR order when
  it matters) — then stop. Approval must come fresh in the moment; a plan
  that mentions merging does not count as approval to merge.
